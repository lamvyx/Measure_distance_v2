# Hướng dẫn sử dụng
- tạo môi trường ảo: `python -m venv venv`
- dùng môi trường ảo: `.\venv\Scripts\activate`
- cài đặt thư viện: `pip install -r requirements.txt`
- chạy code : `py measure_distance.py`

# TÍNH_KHOANG_CACH_CAMERA

dự án của nhóm về tính khoảng cách của vật thể dựa vào chỉ 1 camera. là dự án về môn trí tuệ nhân tạo iuh
